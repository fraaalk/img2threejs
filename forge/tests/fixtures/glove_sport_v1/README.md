# License-safe Sport Gloves golden fixture

Run `python3 forge/tests/fixtures/glove_sport_v1/build_fixture.py` to regenerate the four ordered
views. The fixture is intentionally synthetic so tests never depend on an external reference or
an unresolved license. Its metadata records the view roles and stable hashes after generation.
